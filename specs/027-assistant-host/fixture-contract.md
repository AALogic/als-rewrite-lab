# Fixture Contract 027: AssistantHost v0.2

Status: accepted
Date: 2026-08-05

Static presentation fixtures cover arrival, one queued item, a long scrollable
queue, processing, ready-complete, ready-incomplete, parcel armed, dragging and
delivered. Every fixture uses display-safe names. Layout assertions prove that
the character canvas stays 96 by 128 pixels and queue text cannot overlap the
ground line or controls. Reduced-motion fixtures retain status text and a
recognizable state frame.
