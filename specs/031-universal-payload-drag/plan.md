# Plan 031: UniversalPayloadDrag

Status: accepted, v0.2
Date: 2026-08-06

1. Split fixed provider opening from native drag arming.
2. Advance module 028 to a multi-directory transfer attempt.
3. Add provider-independent drag IPC and path-free events.
4. Replace the whole-character drag image with a parcel-only asset.
5. Build all directory pasteboard items from backend-owned attempt paths.
6. Keep the existing real-AppKit-`mouseDown` surface technique.
7. Verify Finder manually; retain WeTransfer as a separate manual gate.
8. Bind drag start and terminal outcomes to the exact collection handoff.
9. Keep provider opening non-terminal; only an accepted native drop completes
   the active order.
