# Fixture Contract: 010 StagingExecutor

Tests create temporary source files and an absent staging path. They never use
or alter real Ableton projects. Fixtures cover a valid two-file plan, wrong
hash, existing staging, traversal, blocked plan, repeat execution, and a
symlink source on Unix.
