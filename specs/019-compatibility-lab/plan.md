# Plan: 019 Compatibility Lab

1. Add a pure structural compatibility assessment over `ALSReadModel`.
2. Add an experimental current-path planning mode that reuses existing rewrite
   profiles but does not require the Live 11.3 document label.
3. Thread compile-time capability and explicit consent through desktop
   application requests.
4. Extend redacted diagnostics and add final compatibility-report assembly.
5. Add Compatibility Lab UI controls and manual Ableton result capture.
6. Add a separate Tauri configuration and manually triggered Windows workflow.
7. Prove strict-build regression, lab fail-closed behavior and report privacy.
