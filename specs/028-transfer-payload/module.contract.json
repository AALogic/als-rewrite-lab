{
  "id": "028-transfer-payload",
  "status": "ready",
  "guard_version": "0.2",
  "engineering_rules": {"document": "ENGINEERING_RULES.md", "version": "0.1", "required_sections": ["Purpose", "Clean Code", "Modularity", "Testability", "Error Handling", "Data Safety", "Project Hard Rules"]},
  "required_docs": ["spec.md", "plan.md", "tasks.md", "fixture-contract.md"],
  "required_spec_literals": ["TransferPayloadAttempt v0.2", "DeliveryCollectionSnapshot v0.1", "no attempt -> armed -> dragging -> no attempt", "terminal attempt", "outside module scope"],
  "blocking_markers": ["BLOCKING_UNKNOWN", "Gate Status: UNKNOWN", "Gate Status: AMBIGUOUS", "Gate Status: CONFLICTING"],
  "hidden_uncertainty_gate": true,
  "required_checked_obligations": true,
  "required_obligations": [
    "DOCUMENTED_ONLY: TransferPayload owns no provider or browser behavior",
    "DOCUMENTED_ONLY: React no longer supplies an authoritative payload path",
    "DOCUMENTED_ONLY: terminal attempts preserve the candidate for explicit retry",
    "DOCUMENTED_ONLY: candidate binds one immutable collection revision",
    "REVIEW_ONLY: payload validation never enumerates or mutates directory contents",
    "REVIEW_ONLY: one invalid collection member blocks the whole attempt",
    "ENFORCED_BY_TEST: latest_collection_revision_is_the_only_payload_candidate",
    "ENFORCED_BY_TEST: multi_directory_attempt_preserves_snapshot_order",
    "ENFORCED_BY_TEST: stale_collection_revision_is_rejected",
    "ENFORCED_BY_TEST: invalid_member_blocks_entire_drag_attempt",
    "ENFORCED_BY_TEST: concurrent_payload_attempt_is_rejected",
    "ENFORCED_BY_TEST: payload_attempt_is_one_shot",
    "ENFORCED_BY_TEST: terminal_drag_preserves_candidate_for_retry",
    "ENFORCED_BY_TEST: explicit_retry_reset_clears_only_the_attempt",
    "ENFORCED_BY_TEST: reset_clears_hung_attempt_and_candidate",
    "ENFORCED_BY_TEST: payload_errors_are_path_free",
    "ENFORCED_BY_TYPE: TransferPayloadAttempt"
  ],
  "required_tests": ["latest_collection_revision_is_the_only_payload_candidate", "multi_directory_attempt_preserves_snapshot_order", "stale_collection_revision_is_rejected", "invalid_member_blocks_entire_drag_attempt", "concurrent_payload_attempt_is_rejected", "payload_attempt_is_one_shot", "terminal_drag_preserves_candidate_for_retry", "explicit_retry_reset_clears_only_the_attempt", "reset_clears_hung_attempt_and_candidate", "payload_errors_are_path_free"],
  "required_tests_must_not_be_ignored": true,
  "required_tests_must_have_substance": true,
  "public_contract_mode": "exact",
  "public_contract": {"TransferPayloadAttempt": ["attempt_id", "collection_id", "collection_revision", "target_roots"]},
  "expected_source_files": ["apps/rescue-desktop/src-tauri/src/transfer_payload.rs", "apps/rescue-desktop/src-tauri/src/transfer_payload_tests.rs"],
  "dependency_manifest_paths": ["apps/rescue-desktop/src-tauri/Cargo.toml"],
  "quality_source_globs": ["apps/rescue-desktop/src-tauri/src/transfer_payload.rs"],
  "guarded_source_globs": ["apps/rescue-desktop/src-tauri/src/transfer_payload.rs"],
  "forbidden_output_fields": ["provider_id", "provider_url", "upload_status", "share_link"],
  "forbidden_guarded_source_patterns": [
    {"literal": "std::fs::copy", "label": "folder copy policy"},
    {"literal": "remove_dir", "label": "directory deletion"},
    {"literal": "remove_file", "label": "file deletion"},
    {"literal": "wetransfer", "label": "provider coupling"},
    {"literal": "open_url", "label": "browser effect"},
    {"literal": "unwrap(", "label": "unwrap"},
    {"literal": "expect(", "label": "expect"}
  ],
  "allowed_dependency_names": ["rescue_application", "serde", "serde_json", "tauri", "tauri-build", "tauri-plugin-dialog", "tauri-plugin-opener", "tauri-plugin-single-instance", "tempfile", "windows-sys"],
  "quality_limits": {"max_source_file_lines": 300, "max_function_lines": 75}
}
