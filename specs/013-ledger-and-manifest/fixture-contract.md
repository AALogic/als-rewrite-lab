# Fixture Contract: 013 PrivateLedger / PackageManifest

Temporary fixtures contain a complete in-memory plan/staging/rewrite/validation
run and a small staging tree. Absolute paths are unique to the temporary root,
which makes privacy assertions concrete. No user project is used.
