# Fixture Contract: 014 PackagePromoter

Temporary fixtures create source files, validated staged files, portable
manifest, and private ledger. Every promotion target is under a temporary root.
Tests never move or remove user files.
