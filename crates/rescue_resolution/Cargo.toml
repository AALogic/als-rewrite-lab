[package]
name = "rescue_resolution"
version = "0.1.0"
edition = "2021"

[dependencies]
rescue_analyzer = { path = "../rescue_analyzer" }
rescue_catalog = { path = "../rescue_catalog" }
serde = { version = "1", features = ["derive"] }
