[package]
name = "rescue_analyzer"
version = "0.1.0"
edition = "2021"

[dependencies]
rescue_core = { path = "../rescue_core" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
